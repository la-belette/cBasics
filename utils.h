#ifndef CBASICS_UTILS_H
#define CBASICS_UTILS_H

#include <stddef.h>

/*
 * print_int_array prints the size first elements of the integer array array.
 * @param array is a pointer to the first elements of the array to print
 * @param size is the number of element(s) to print
 */
void print_int_array(int *array, size_t size);

#endif //CBASICS_UTILS_H
